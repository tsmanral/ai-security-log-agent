interface:
  display_name: "Ai Security Log Agent"
  short_description: "Repo-specific patterns and workflows for ai-security-log-agent"
  default_prompt: "Use the ai-security-log-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true