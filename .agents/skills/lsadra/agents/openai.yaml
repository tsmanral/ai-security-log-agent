interface:
  display_name: "Lsadra"
  short_description: "Repo-specific patterns and workflows for lsadra"
  default_prompt: "Use the lsadra repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true